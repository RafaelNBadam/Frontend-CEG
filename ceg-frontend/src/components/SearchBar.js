import 'src/styles/SearchBar.css';

const SearchBarContainer = () => {
  return (
    <div>
      <input className='SearchBar' type="text" placeholder="Pesquise cursos..." />
    </div>
  );
}

export default SearchBarContainer;